function validateAdapterOptions(adapterType: string, adapterOptions?: object) {
  // TODO: Implement this function
  adapterType = adapterType.toLowerCase();
  return adapterOptions;
}

export default validateAdapterOptions;
